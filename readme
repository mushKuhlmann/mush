 source venv_env2/bin/activate
/.git/config

#посмотреть какие есть ветки. напротив текущей ветки будет *
 git branch
#сменить ветку, указать имя ветки
git checkout branch1
#создать новую ветку, указать имя ветки
git branch branch2
#после создания в этой новой ветке находится как бы копия всех файлов той ветки
# из которой эту ветку создавали.

#переименовать ветку
git branch --move branch2 branch2-newname

#посмотреть все дерево
 git log --oneline --decorate --graph --all
#слить ветки. следует делать из той ветки в которую требуется сделать слияние
# указать ту ветку которую надо слить с текущей
git checkout branch1
git merge branch-newname
#теперь в родительской ветке branch1 есть все изменения, которые были в
# дочерней branch-newname. теперь доченюю можно удалить
git branch -d branch-newname

#Если в разных ветках один и тот же файл был изменен, то будет конфликт при
# слиянии, этот конфликт нужно будет устранять вручную, а затем снова сливать

#посмотреть коммиты в ветках
git branch -v


#созданную ветку отправить на удаленный гит
git push -u origin vetka2

#теперь можно делать изменения в этой ветке и они будут пушиться
#в нужную ветку на удаленном гите. можно и в родительской, тогда будет
#пушиться в родительскую

#add string
